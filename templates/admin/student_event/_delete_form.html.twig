<form method="post" action="{{ path('student_event_delete', {'id': stevent.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <label for="assigned">retirer l'assignement de l'utilisateur</label>
    <input type="checkbox" name="assigned">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ stevent.id) }}">
    <button class="btn">Delete</button>
</form>
